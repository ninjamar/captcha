{
  "name": "captcha",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.2.11",
    "vitest": "1.6.1",
    "wrangler": "^3.57.0"
  },
  "dependencies": {
    "@tsndr/cloudflare-worker-router": "^3.2.4",
    "workers-og": "^0.0.24"
  }
}
